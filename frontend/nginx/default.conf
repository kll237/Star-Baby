# DEPRECATED — 此文件已弃用，请勿使用。
# 公网部署改用模板 + 注入脚本：
#   frontend/nginx/http.conf.template   （80：ACME 验证 + 跳转 443）
#   frontend/nginx/https.conf.template  （443：业务，证书存在时启用）
#   frontend/nginx/entrypoint.sh       （启动时把 ${DOMAIN} 注入生成 conf.d 配置）
# Dockerfile 已不再 COPY 本文件。
