# Eclipse Mosquitto 配置（阶段八：真实 MQTT 跨进程联动）
# 仅用于内部演示/编排环境；生产环境请开启 TLS 与账号认证（password_file / listener ... 8883）。

# 监听 TCP 1883
listener 1883
allow_anonymous true

# 关闭持久化，避免容器重启残留会话（演示环境足够）
persistence false

# 日志输出到 stdout，便于 docker logs 查看
log_dest stdout
log_type all
